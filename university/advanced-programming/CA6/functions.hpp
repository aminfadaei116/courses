#include "node.hpp"
#include <vector>

Node* find_node(vector <Node*>, size_t);
int find_node_idx(vector <Node*>, size_t);
vector <int> extract(vector <Node*>);
vector <int> sorted_number(vector <int>);